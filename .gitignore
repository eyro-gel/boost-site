# macOS
.DS_Store
._*
.Spotlight-V100
.Trashes

# editors
.vscode/
.idea/
*.swp

# local scratch — never commit working files
_verify/
_export*/
_probe.html
_nojs*.html
_og.html
_strip.html
_team.html

# internal — never deploy. These are working documents, not site files;
# anything matching this stays out of the repo and therefore out of Pages.
_internal/
*-internal.*
